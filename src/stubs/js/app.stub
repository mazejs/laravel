import mazejs from "mazejs";
import moment from "moment";
import vue from "vue";

window.Vue = vue;

Vue.prototype.bus = new Vue();
Vue.prototype.moment = moment;
Vue.use(mazejs);

require("./bootstrap");

const app = new Vue({
    el: "#app",
});
